import { FilterOffByPropertyPipe } from './filter-off-by-property.pipe';

describe('FilterOffByPropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOffByPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
